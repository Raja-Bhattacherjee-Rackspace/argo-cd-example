apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: guestbook
  namespace: argocd
spec:
  destination:
    namespace: guestbook
    server: "https://kubernetes.default.svc"
  project: default
  source:
    repoURL: "https://github.com/Raja-Bhattacherjee-Rackspace/argo-cd-example.git"
    targetRevision: HEAD
    path: manifests/QA/guestbook-application
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true